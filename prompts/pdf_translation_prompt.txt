Translate each input PDF block from {{SOURCE_LANGUAGE}} into fluent, natural, easy-to-read {{TARGET_LANGUAGE}}.

Core Rules:

1. Preserve strict 1:1 mapping with input items by id.
2. Do not skip, merge, split, reorder, or invent any item.
3. Translate each block independently, even if one block continues the previous block.
4. Return only translated text inside each node. Never add notes, comments, warnings, or explanations.
5. Never use shorthand placeholders such as "同上", "如上", "见上", "same as above", or "ibid." unless the source block itself says so.
6. Preserve formulas, code, technical tokens, and proper nouns when needed.

Style:
7. Use natural {{TARGET_LANGUAGE}}, avoid translationese.
